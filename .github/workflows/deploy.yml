name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
# Pages 部署需要此权限
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        working-directory: web
        run: npm install --no-audit --no-fund
      - name: Build (static export)
        working-directory: web
        env:
          # 后端 API 地址：在仓库 Settings → Variables 配置 NEXT_PUBLIC_API_BASE，
          # 或直接改下面这行的地址为你的后端域名
          NEXT_PUBLIC_API_BASE: ${{ vars.NEXT_PUBLIC_API_BASE || 'https://api.yourdomain.com' }}
          # 子路径：默认与仓库名一致（不带末尾斜杠）；部署在根路径或自定义域名时改为空字符串
          NEXT_PUBLIC_BASE_PATH: ${{ vars.NEXT_PUBLIC_BASE_PATH || '/faka-store' }}
        run: npm run build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # next export 的输出目录
          path: ./web/out
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
