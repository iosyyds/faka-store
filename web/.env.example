# 后端 API 地址（必填）
# 在 GitHub Actions 中作为变量 NEXT_PUBLIC_API_BASE 配置，
# 或复制本文件为 .env.local 在本地构建时使用。
NEXT_PUBLIC_API_BASE=https://api.yourdomain.com

# 如果你的 Pages 部署在 https://用户名.github.io/仓库名/ 子路径下：
# NEXT_PUBLIC_BASE_PATH=/faka-store  （注意不带末尾斜杠）
